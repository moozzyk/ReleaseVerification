# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

target 'SignalRChatPhoneTest' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
# `pod repo update` before updating
  pod 'SwiftSignalRClient', '~> 0.9.1'
# `pod update --clean-install`
#  pod 'SwiftSignalRClient', :git => 'https://github.com/moozzyk/SignalR-Client-Swift'
end
